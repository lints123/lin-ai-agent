spring:

  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY:your-dashscope-api-key}
      chat:
        options:
          model: qwen-plus

ai:
  models:
    default-model: qwen
    nvidia:
      base-url: https://integrate.api.nvidia.com
      api-key: ${NVIDIA_API_KEY:your-nvidia-api-key}
      model: meta/llama-3.1-70b-instruct
